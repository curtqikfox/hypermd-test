{
  "name": "hypermd-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "codemirror": "^5.65.14",
    "qik-hypermd-fork": "^3.11.24",
    "react": "^16.4.0",
    "react-dom": "^16.14.0",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build; npm run build-submodule",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build-submodule": "cd src/qik-hyper-md && npm run build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
